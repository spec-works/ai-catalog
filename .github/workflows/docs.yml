name: Deploy Documentation

on:
  push:
    branches: [main]
    paths:
      - 'docs/site/**'
      - 'dotnet/src/**'
      - 'README.md'
      - '.github/workflows/docs.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Download shared template
        run: |
          curl -L -o template.zip https://github.com/spec-works/spec-works.github.io/archive/refs/heads/main.zip
          unzip -q template.zip
          mkdir -p docs/site/template
          cp -r spec-works.github.io-main/docs/template/* docs/site/template/
          rm -rf spec-works.github.io-main template.zip

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'

      - name: Install DocFX
        run: dotnet tool install -g docfx

      - name: Build documentation
        run: docfx docs/site/docfx.json

      - name: Deploy to gh-pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: docs/site/_site
          force_orphan: true
